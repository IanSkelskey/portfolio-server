# Port the server listens on (Render sets this automatically)
PORT=3000

# Your Resend API key — https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# The domain you verified in Resend (used as the From address)
# e.g. "ianskelskey.com" → sends from contact@ianskelskey.com
RESEND_DOMAIN=yourdomain.com

# The email address that receives contact form submissions
CONTACT_TO_EMAIL=you@example.com

# Set to "development" locally to also allow localhost:5173 via CORS
NODE_ENV=development
